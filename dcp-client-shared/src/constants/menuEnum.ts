export enum TriggerEnum {
  NONE = "NONE",
  FOOTER = "FOOTER",
  CENTER = "CENTER",
  HEADER = "HEADER",
}

export enum MenuModeEnum {
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
  VERTICAL_RIGHT = "vertical-right",
  INLINE = "inline",
}

export enum MixSidebarTriggerEnum {
  HOVER = "hover",
  CLICK = "click",
}

export enum EnumInternalMenu {
  INTERNAL_PLATFORM = 0,
  EXTERNAL = 1,
  INTERNAL_PROJECT = 2,
}

export enum EnumMenuMode {
  INNER = "INNER",
  OUTER = "OUTER",
}

export enum EnumAccessMethod {
  Normal = "normal",
  Micro = "micro",
  Iframe = "iframe",
}

export enum EnumMenuType {
  DOMAIN = "DOMAIN",
  MENU = "MENU",
}
